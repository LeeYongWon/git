doctype html
html
   head
      meta(charset='utf-8')
   body
      h1
         a(href='/topic') Server Side Javascript edit
      ul   
         each s_topic in S_topic
            li
               a(href='/topic/'+s_topic.title)= s_topic.title
      artcle
         form(action='/topic/'+I_topic.id+'/edit' method='post')
            p
               input(type='text', name='title', placeholder='title', value=I_topic.title)
            p
               textarea(name='description' placeholder='description')= I_topic.description
            p
               input(type='text', name='author', placeholder='author', value=I_topic.author)
            p
               input(type='submit')
      ul
         li
            a(href='/topic/add') add
         
