doctype html
html
   head
      meta(charset='utf-8')
   body
      h1
         a(href='/topic') Sever Side Javacript
      ul
         each s_topic in S_topic
            li
               a(href='/topic/'+s_topic.id)= s_topic.title
      artcle
         if I_topic
            h2= I_topic.title
            = I_topic.description
            div= 'by '+I_topic.author
         else
            h2 Welcome
            | This is Serverside javascript tutorial.
      ul
         li
            a(href='/topic/add') add
